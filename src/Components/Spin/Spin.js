import { TailSpin } from 'react-loader-spinner';

const Spin = () => {
  return (
    <div>
      <TailSpin color="#00BFFF" height={80} width={80} visible={false} />
    </div>
  );
};

export default Spin;
